Big Data application for Real Estate.

# Setup

To get everything set-up run the following code on your terminal. You must have python 3.13 in your computer.

```
python3.13 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip
pip install -r requirements.txt
````
